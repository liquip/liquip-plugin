allprojects {
    group = "io.github.liquip"
    version = "2.0.2-beta"

    repositories {
        mavenCentral()
        maven { url = uri("https://repo.papermc.io/repository/maven-public/") }
    }
}
